/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    distDir: "dist",
    cleanDistDir: true,
    assetPrefix: "https://drinks-co.vercel.app"
}

module.exports = nextConfig
